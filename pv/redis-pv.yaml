apiVersion: v1
kind: PersistentVolume
metadata:
  name: gitlab-redis
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  storageClassName: nfs  
  nfs:
    path: /mnt/nvme/kubernetes/redis/
    server: 10.10.10.2
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-redis
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi    
